.tooltip-wrap {
  position: relative;
}

.tooltip-wrap .tooltip {
  z-index: 10;
  background-color: var(--backgroundColor);
  box-shadow: 0px 0px 3px var(--borderColor);
  border-radius: 12px;
  width: 165px;
  font-size: 12px;
  line-height: 12px;
}
